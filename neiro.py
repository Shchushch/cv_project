import torch
# Здесь импорт нужной модели

gan = "TheModelClass()" # Здесь экземпляр модели
#gan.load_state_dict(torch.load("PATH"))
tumor ="TheModelClass()" # Здесь экземпляр модели
wind = "TheModelClass()" # Здесь экземпляр модели
denoised = "TheModelClass()" # Здесь экземпляр модели

def get_digit(img):
    pass
    # gan.eval()
    # device = "mps"#torch.device("cuda" if torch.cuda.is_available() else 'cpu')
    # res_img = "gan.predict(img)"
    # return res_img

def get_tumor(img):
    pass
    # tumor.eval()
    # device = "mps"#torch.device("cuda" if torch.cuda.is_available() else 'cpu')
    # res_img = "tumor.predict(img)"
    # return res_img
def get_wind(img):
    pass
    # gan.eval()
    # device = "mps"#torch.device("cuda" if torch.cuda.is_available() else 'cpu')
    # res_img = "wind.predict(img)"
    # return res_img
def get_denoised(img):
    pass
    # denoised.eval()
    # device = "mps"#torch.device("cuda" if torch.cuda.is_available() else 'cpu')
    # res_img = "denoised.predict(img)"
    # return res_img